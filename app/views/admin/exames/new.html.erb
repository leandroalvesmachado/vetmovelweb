<% content_for :breadcrumb do %>
  <%= render Admin::BreadcrumbComponent.new do |component| %>
    <% component.with_title do %> 
      <i class="fas fa-ambulance"></i> Exames
    <% end %>
    <% component.with_link(name: 'Exames', link: admin_exames_path) %>
    <% component.with_link(name: 'Novo Exame', link: '#') %>
  <% end %>
<% end %>

<%= 
  render partial: 'form', locals: {
    title: 'Exame',
    subtitle: 'Novo',
    action: create_admin_exames_path,
    method: 'POST',
    model: @exame,
    back_url: admin_exames_path,
    submit_text: 'Salvar'
  } 
%>