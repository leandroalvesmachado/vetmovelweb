<% content_for :breadcrumb do %>
  <%= render Admin::BreadcrumbComponent.new do |component| %>
    <% component.with_title do %> 
      <i class="fas fa-ambulance"></i> Cirurgias
    <% end %>
    <% component.with_link(name: 'Cirurgias', link: admin_cirurgias_path) %>
    <% component.with_link(name: 'Editar Cirurgia', link: '#') %>
  <% end %>
<% end %>

<%= 
  render partial: 'form', locals: {
    title: 'Cirurgia',
    subtitle: 'Edição',
    action: update_admin_cirurgias_path(@cirurgia),
    method: 'put',
    model: @cirurgia,
    back_url: admin_cirurgias_path,
    submit_text: 'Atualizar'
  } 
%>